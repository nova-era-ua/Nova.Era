{
	"$schema": "../../../@schemas/model-json-schema.json#",
	"schema": "cat",
	"model": "Tags",
	"dialogs": {
		"settings": {
			"template": "settings.template",
			"view": "settings.dialog",
			"parameters": {
				"For": "{{Params.For}}"
			}
		}
	}
}