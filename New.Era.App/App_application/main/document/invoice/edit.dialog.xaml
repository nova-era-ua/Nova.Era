<Dialog xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml" Size="Max">
	<Dialog.Buttons>
		<Button Content="@[SaveAndClose]" Style="Primary" Command="{BindCmd SaveAndClose}"/>
		<Button Content="@[Cancel]" Command="{BindCmd Close}" />
	</Dialog.Buttons>
	<Dialog.Taskpad>
		<Taskpad Collapsible="False">
			<ComboBox Label="@[Operation]" Value="{Bind Document.Operation}" ItemsSource="{Bind Operations}">
				<ComboBoxItem Content="{Bind Name}" Value="{Bind}"/>
			</ComboBox>
		</Taskpad>
	</Dialog.Taskpad>

	<TextBox Label="@[Memo]" Value="{Bind Document.Memo}" Multiline="True"/>
</Dialog>