<Dialog xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml" Size="Max" Padding="1rem">
	<Dialog.Buttons>
		<Button Content="Провести" Command="{BindCmd Execute, CommandName=apply}"/>
		<Separator />
		<Button Content="@[SaveAndClose]" Style="Primary" Command="{BindCmd SaveAndClose}"/>
		<Button Content="@[Cancel]" Command="{BindCmd Close}" />
	</Dialog.Buttons>
	<Dialog.Taskpad>
		<Taskpad Width="20rem" Collapsible="False">
			<Static Label="Операція" Value="{Bind Document.Operation.Name}"/>
		</Taskpad>
	</Dialog.Taskpad>
	<Header Content="Видаткова накладна" />
	<Grid Padding="0" Columns="15rem,1*">
		<DatePicker Label="Дата" Value="{Bind Document.Date}"/>
	</Grid>
</Dialog>